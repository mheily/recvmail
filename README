BUILDING
--------

On FreeBSD 10.2:

  $ sudo pkg install autotools pkgconf libevent2
  $ autoreconf -fvi
  $ ./configure
  $ make

INSTALLATION
------------

# make install

# Create a recvmail user and group
Linux: sudo adduser --system --no-create-home --group recvmail
FreeBSD: pw group add recvmail ; pw user add recvmail -g recvmail -s /nonexistent -m

# Create the mailstore
sudo -u recvmail mkdir /home/recvmail/store

# Create the spool directory
sudo -u recvmail mkdir -p /home/recvmail/spool/{new,cur,tmp}

# Create a domain
sudo -u recvmail mkdir /home/recvmail/store/example.com

# Create a mailbox
sudo -u recvmail mkdir -p /home/recvmail/store/example.com/test/{new,cur,tmp}

RUNNING
-------

# To run in testing mode
sudo ./recvmail -fv

# Using the provided rc(8) script for FreeBSD
sudo service recvmail start

TESTING
-------

printf "helo a\nmail from:<b@c.com>\nrcpt to:<test@example.com>\ndata\nSubject: hello\n\nHi\n.\nquit\n" | nc 10.0.2.15 25
